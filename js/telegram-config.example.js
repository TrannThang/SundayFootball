/* ==========================================================================
   SUNDAY FOOTBALL - TELEGRAM BOT NOTIFICATION CONFIG (EXAMPLE)
   This is a template, NOT the real config - js/telegram-config.js (git-ignored)
   is generated automatically at deploy time from Vercel Environment Variables
   TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID (see scripts/generate-telegram-config.js).
   For local testing, copy this file to js/telegram-config.js and fill in real values.
   ========================================================================== */

window.TELEGRAM_CONFIG = {
  botToken: "PASTE_BOT_TOKEN_HERE",
  chatId: "PASTE_CHAT_ID_HERE"
};
